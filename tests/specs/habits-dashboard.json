[
  {
    "name": "Dashboard has a welcoming empty state",
    "steps": [
      { "send": { "callback": "habits:dashboard" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits yet — tap ➕ Create habit to build your first one." } }] }
    ]
  }
]
