[
  {
    "name": "Weekly recap has a welcoming empty state",
    "steps": [
      { "send": { "callback": "recap:weekly" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits to recap yet — tap ➕ Create habit and I’ll track your wins." } }] }
    ]
  }
]
